# Generated design dossier (built by site/build.sh)
site/bosun-design.html

# Spago / PureScript
output/
output-go/
.spago/
.psci_modules/
node_modules/
.psc-ide-port

# spec-node failure-rerun cache
.spec-results

# Bosun's Chair bundle (build artifact: spago bundle -p bosun-chair)
chair/app.js

# printed/exported doc artifacts (generated from the .md sources)
docs/*.pdf

# Working-tree backups (atomic-write .bak from chair-server fleet edits, plus
# named one-off backups taken before risky migrations). The git history is the
# real long-tail audit; these are belt-and-braces for the current session only.
registry/*.bak
registry/*.bak-*

# macOS
.DS_Store
.direnv/
