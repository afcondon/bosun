module Bosun.Adapters where

-- | Placeholder. Per-source `ingest`/`emit` modules (compose, registry,
-- | plist) land here in Phase 3 — each a pure parser into the core IR
-- | and renderer back out of it.
