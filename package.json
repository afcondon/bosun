{
  "name": "bosun",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A typed deployment-configuration reconciler.",
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "bosun": "spago run -p bosun-cli",
    "chair-server": "spago build && node chair-server/run.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
